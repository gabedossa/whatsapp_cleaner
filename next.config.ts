import type { NextConfig } from "next";

// Configuração do Next.js
const nextConfig: NextConfig = {
  // Permite importar fontes do Google Fonts via next/font
  reactStrictMode: true,
};

export default nextConfig;
